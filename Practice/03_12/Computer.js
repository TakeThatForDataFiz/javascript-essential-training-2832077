class Computer {
  constructor(CPU, MotherBoard, Monitor, GPU) {
    this.CPU = CPU;
    this.Motherboard = MotherBoard;
    this.Monitor = Monitor;
    this.GPU = GPU;
  }
}

export default Computer;
